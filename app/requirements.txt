# DFIR Log Sorter Requirements
# This application uses only Python standard library modules
# No external dependencies required

# Python 3.7+ required for:
# - tkinter (GUI framework)
# - datetime (timestamp parsing)
# - csv (export functionality) 
# - json (export functionality)
# - typing (type hints)
# - re (regex for timestamp parsing)

# If running on a minimal Python installation, you may need:
# python3-tk (on Ubuntu/Debian)
# tkinter (usually included with Python on Windows/macOS)
